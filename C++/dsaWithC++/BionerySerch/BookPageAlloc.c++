// Book page alocaten
#include <iostream>
using namespace std;
int Search(int arr[],int n){
    int size;
    for(int i=0;i<sizeof(arr)/sizeof(int);i++){
        size=arr[i];
    }
    int s=0,e=size;
    int mid=s+(e-s)/2;
    while(s<=e){
        if
    }
}
int main(){
    int book[5]={10,20,30,40,50},n=3;
    cout << Search(book , n);
}